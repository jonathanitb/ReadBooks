<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryDark">@color/powerred</item>
        <item name="colorAccent">@color/powerred</item>
    </style>
    <style name="listfont"
       >
        <item name="android:fontFamily">@font/p5hatty</item>
    </style>

</resources>